// Including necessary libraries
#include <iostream>
#include <math.h>

// Declaring and Initializing the row and column counts of matrix
const int row = 5;
const int col = 5;

// Declaring all functions we'll use in project
int length(int);

void printMatrix(int[][col]);

int DecimalToBinary(int);

int sumUp(int[][col]);
int sumDown(int[][col]);

void changeUp(int[][col], int);
void changeDown(int[][col], int);

void change(int[][col]);
